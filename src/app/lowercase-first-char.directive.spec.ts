import { LowercaseFirstCharDirective } from './lowercase-first-char.directive';

describe('LowercaseFirstCharDirective', () => {
  it('should create an instance', () => {
    const directive = new LowercaseFirstCharDirective();
    expect(directive).toBeTruthy();
  });
});
